# Local environment example (placeholders only)
# Copy this file to ".env.local" and fill real values locally (never commit ".env.local")

# App
NEXT_PUBLIC_SITE_NAME=LevelUp
NEXT_PUBLIC_APP_ENV=development

# Supabase (EU dev project)
NEXT_PUBLIC_SUPABASE_URL=https://YOUR-PROJECT-ref.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=YOUR-ANON-KEY
# Server-only: never expose in browser
SUPABASE_SERVICE_ROLE_KEY=YOUR-SERVICE-ROLE-KEY

# Anthropic (server-only, added after scaffold or when ready)
ANTHROPIC_API_KEY=YOUR-ANTHROPIC-KEY
ANTHROPIC_MODEL=claude-sonnet-4-20250514